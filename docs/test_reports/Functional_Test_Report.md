# Segar 测试报告

## 1. 测试环境

| 项目 | 内容 |
|------|------|
| 硬件 | x86_64 / orin |
| 操作系统 | ubuntu22.04 |
| 测试版本 | Segar 2.0.0 |
| 测试日期 | 2026/2/11 |

## 2. 测试结论

**通过**

## 3. 测试用例

| 用例编号 | 用例简介 | 测试流程 | 通过条件 | 测试结果 |
|----------|----------|----------|----------|----------|
| SegarTestCase001 | Action 同步发送 Goal 并等待结果 | 1. 创建 Action Server 节点与 Action Client 节点，并校验节点创建成功。<br>2. 在 Server 节点上注册 DemoAction，配置 on_goal/on_cancel/on_execute：on_execute 内按 goal.exec_times() 循环执行，每次发送 Feedback，最后 Succeed 返回 result（含 total 与 full_sequence）。<br>3. 在 Client 节点上创建同名 Action Client，WaitForService 后构造 Goal（exec_times=5），调用同步 SendGoal 并等待返回。<br>4. 校验返回的 Goal 状态为 SUCCEEDED；校验 result.total() 与 result.detail().full_sequence().size() 等于 exec_times；校验 Feedback 回调被调用次数不少于 exec_times。 | 1. Server/Client 节点创建成功，Action Server/Client 创建成功。<br>2. Client 能成功发送 Goal 并得到同步返回。<br>3. 结果状态为 SUCCEEDED。<br>4. result.total() 与 full_sequence 长度与 exec_times 一致。<br>5. 收到的 Feedback 次数 ≥ exec_times。 | PASS |
| SegarTestCase002 | Action 异步发送 Goal 并等待结果 | 1. 同 001，创建 Server/Client 节点及 DemoAction Server（on_execute 中发 Feedback 后 Succeed）。<br>2. Client 使用 AsyncSendGoal 发送 Goal，通过 on_result 回调接收结果，通过 on_feedback 统计反馈次数；主流程在循环中等待 result_received 标志或超时 5 秒。<br>3. 在 on_result 中记录 received_status、received_result；主流程校验 result_received 为 true，received_status 为 SUCCEEDED，received_result.total() 与 full_sequence 正确。<br>4. 校验 feedback_count ≥ exec_times。 | 1. Server/Client 与 Action Server/Client 创建成功。<br>2. AsyncSendGoal 调用成功，on_result 被调用。<br>3. received_status 为 SUCCEEDED；received_result 的 total 与 full_sequence 与 goal 一致。<br>4. 反馈次数 ≥ exec_times。 | PASS |
| SegarTestCase003 | Action 同步场景下取消 Goal | 1. 创建 Server/Client 节点；Server 的 on_execute 中在循环内检查 cancel_requested，若为 true 则构造部分 result 并调用 CancelGoal(goal_id, result) 返回。<br>2. Client 同步 SendGoal 后，在另一线程或定时后调用 CancelGoal，再等待同步 SendGoal 返回。<br>3. 校验返回状态为取消相关状态（如 CANCELED）；校验 result 中 total 与 sequence 为取消前的部分结果。 | 1. Server 能接受 Goal 并在执行中响应取消。<br>2. Client 调用 CancelGoal 后收到取消结果。<br>3. 结果状态表示已取消；部分 result（total、sequence）与执行进度一致。 | PASS |
| SegarTestCase004 | Action 异步场景下取消 Goal | 1. 与 003 类似，Server 在 on_execute 中响应 cancel_requested 并 CancelGoal。<br>2. Client 使用 AsyncSendGoal，在适当时机调用 CancelGoal；在 on_result 回调中记录 status 与 result。<br>3. 校验 on_result 被调用，status 为取消状态，result 为部分结果。 | 1. 异步 SendGoal 与 CancelGoal 调用成功。<br>2. on_result 被调用且 status 表示已取消。<br>3. result 中 total/sequence 为取消时的部分结果。 | PASS |
| SegarTestCase005 | ROS2 录制与回放 | 1. 启动 ROS2 demo talker 进程，发布到 rt/chatter（String 类型）。<br>2. 调用 segar record 命令，指定输出 record 文件，录制一段时间后停止。<br>3. 读取 record 文件，解析头部（version、message_number、topic_number 等）与索引区，得到 topic_info。<br>4. 调用 segar play 命令回放该 record 文件。<br>5. 在回放侧订阅对应 topic，统计接收到的消息数量与内容，与录制侧预期（或 record 中 message_number/topic 信息）对比。 | 1. record 文件能成功生成并可被解析。<br>2. 头部与索引解析出的 topic_number、message_number、topic_info 合理。<br>3. 回放能正常启动并发布消息。<br>4. 回放接收到的消息数量与内容与录制一致（或与 record 元数据一致）。 | PASS |
| SegarTestCase006 | Segar IDL 消息录制与回放 | 1. 创建 Segar 节点，创建 Writer 向指定 topic 发布 HelloWorld（IDL）消息，按固定次数循环发布。<br>2. 启动 segar record，录制该 topic，录制完成后得到 record 文件。<br>3. 对 record 文件执行 segar bag info，解析输出的 duration、message_number、topic_number、topic_info（topic 名、消息数、类型）。<br>4. 启动 segar play 回放该 record；创建 Reader 订阅同一 topic，接收回放消息。<br>5. 校验 bag info 解析出的消息条数、duration、topic 类型与预期一致；校验回放接收到的消息条数与内容与录制时发送一致。 | 1. 录制文件生成且可被 bag info 正确解析。<br>2. bag info 中 message_number、topic_number、topic_info（含类型）正确。<br>3. 回放后接收到的消息条数等于录制条数。<br>4. 回放接收到的 HelloWorld 内容与录制发送一致。 | PASS |
| SegarTestCase007 | Segar Proto 消息录制与回放 | 1. 创建 Segar 节点，使用 Proto 类型（如 Chatter）向指定 topic 发布消息，循环发送多条。<br>2. 使用 segar record 录制；使用 segar bag info 解析 record。<br>3. 使用 segar play 回放；创建 Reader 接收回放消息。<br>4. 校验 record 中消息数、topic 类型；校验回放接收条数与内容（如 seq、content、timestamp）与发送一致。 | 1. Proto 消息录制与 bag info 解析正确。<br>2. 回放消息条数与录制一致。<br>3. 回放消息各字段（如 seq、content、timestamp）与发送一致。 | PASS |
| SegarTestCase008 | CLI 帮助与版本 | 1. 执行 segar -h 与 segar --help，检查标准输出中包含 usage、子命令列表等帮助信息。<br>2. 执行 segar --version，检查输出包含版本号信息。<br>3. 执行 segar 未知子命令（如 segar unknown_cmd），检查标准错误或标准输出中有错误提示或 usage 提示。 | 1. -h/--help 输出包含 usage 及命令说明。<br>2. --version 输出包含版本号。<br>3. 未知子命令能给出明确错误或帮助提示。 | PASS |
| SegarTestCase009 | CLI 节点列表与信息 | 1. 在进程中 CreateNode 创建指定名称的节点，并保持节点存活（如持有 node 或运行一段时间）。<br>2. 执行 segar node list，解析输出，检查列表中包含刚创建的节点名。<br>3. 执行 segar node info `<node_name>`，检查输出中包含该节点名及节点相关信息（如 topic、service 等）。 | 1. node list 输出中包含创建的节点名。<br>2. node info `<node_name>` 能输出该节点信息且内容合理。 | PASS |
| SegarTestCase010 | CLI Topic（IDL）相关命令 | 1. 创建 Segar 节点，创建 Writer 向某 topic（如 cli_test_topic）发布 IDL 类型（HelloWorld）消息，后台线程按固定间隔（如 50ms）持续发布，保持节点与发布存活。<br>2. 等待约 500ms 后，并行执行：segar topic list、topic type `<topic>`、topic info `<topic>`、topic hz `<topic>`（超时 5s）、topic bw `<topic>`（超时 5s）、topic echo `<topic>`（超时 5s）。<br>3. 校验 list 输出包含该 topic；type 输出为 IDL 类型名；info 输出包含类型与 writer/reader 信息。<br>4. 校验 hz、bw、echo 命令退出码为 0（正常）或 124（超时），且标准输出非空。<br>5. 停止发布线程。 | 1. topic list 包含该 topic。<br>2. topic type 输出正确的 IDL 类型名。<br>3. topic info 输出类型与 writer/reader 信息且与实际情况一致。<br>4. topic hz 退出码为 0 或 124，输出非空。<br>5. topic bw 退出码为 0 或 124，输出非空。<br>6. topic echo 退出码为 0 或 124，输出非空。 | PASS |
| SegarTestCase011 | CLI Topic（Proto）相关命令 | 1. 创建 Segar 节点，创建 Writer 向某 topic（如 cli_test_proto_topic）发布 Proto 类型（Chatter）消息，后台线程按固定间隔（如 50ms）持续发布。<br>2. 等待约 500ms 后，并行执行：segar topic list、topic type `<topic>`、topic info `<topic>`、topic hz `<topic>`（超时 5s）、topic bw `<topic>`（超时 5s）、topic echo `<topic>`（超时 5s）。<br>3. 校验 list 包含该 topic；type 输出为 Proto 类型路径（如 rti.segar.examples.proto.Chatter）；info 与 topic 类型及参与者一致。<br>4. 校验 hz、bw、echo 命令退出码为 0 或 124，且标准输出非空。<br>5. 停止发布线程。 | 1. topic list 包含该 topic。<br>2. topic type 输出正确的 Proto 类型路径。<br>3. topic info 与当前 topic 类型及参与者一致。<br>4. topic hz 退出码为 0 或 124，输出非空。<br>5. topic bw 退出码为 0 或 124，输出非空。<br>6. topic echo 退出码为 0 或 124，输出非空。 | PASS |
| SegarTestCase012 | CLI Service 相关命令 | 1. 创建 Segar 节点，创建 AddTwoInts 服务端并注册到指定 service 名。<br>2. 执行 segar service list，检查输出包含该 service 名。<br>3. 执行 segar service type `<service_name>`，检查输出为 AddTwoInts 对应类型。<br>4. 执行 segar service info `<service_name>`，检查输出包含类型与 server 信息。 | 1. service list 包含该 service。<br>2. service type 输出正确的 srv 类型。<br>3. service info 输出类型与 server 信息正确。 | PASS |
| SegarTestCase013 | CLI Action 相关命令 | 1. 创建 Segar 节点，创建 DemoAction 的 Action Server 并注册到指定 action 名。<br>2. 执行 segar action list，检查包含该 action 名。<br>3. 执行 segar action type `<action_name>`，检查输出为 DemoAction 类型。<br>4. 执行 segar action info `<action_name>`，检查输出包含类型与 server 信息。 | 1. action list 包含该 action。<br>2. action type 输出正确的 action 类型。<br>3. action info 输出类型与 server 信息正确。 | PASS |
| SegarTestCase014 | CLI 参数相关命令 | 1. 创建 Segar 节点，通过 Segar_Set_Local_Param 设置若干本地参数（如 int、string）。<br>2. 执行 segar param list（若有），检查包含该节点或参数相关信息。<br>3. 执行 segar param get `<node>` `<param>`（或等价命令），检查输出与设置值一致。<br>4. 执行 segar param set 修改参数后再次 get，检查修改生效。 | 1. param 相关命令能正确列出/获取/设置该节点参数。<br>2. get 输出与 set 的值一致。<br>3. set 后再次 get 反映最新值。 | PASS |
| SegarTestCase015 | 本地参数读写（int/string/proto） | 1. 创建 Segar 节点。<br>2. 使用 Segar_Set_Local_Param 设置 int 类型参数，再 Segar_Get_Local_Param 读取，校验值一致。<br>3. 设置 string 类型参数，读取并校验。<br>4. 设置 Protobuf 类型参数（如 param::Header，含 module_name、sequence_num、points 等），用 Segar_Get_Local_Param 按值与按 shared_ptr 两种方式读取，校验各字段与设置一致。 | 1. int/string 参数 set 后 get 值一致。<br>2. Proto 参数 set 后 get（值方式）各字段一致。<br>3. Proto 参数 get（shared_ptr 方式）各字段一致。 | PASS |
| SegarTestCase016 | 远程参数（参数服务） | 1. 创建参数服务端节点，导出或设置若干参数（如从文件加载或 Set_Local_Param）。<br>2. 创建客户端节点，通过参数服务 API（如 GetParameter/SetParameter）按参数名 get/set 远程参数。<br>3. 对多种类型（如 int、string、proto）执行 get/set，校验返回值与设置值一致。<br>4. 校验类型不匹配或不存在参数时行为符合预期（如返回错误或默认值）。 | 1. 客户端能成功 get 服务端导出的参数且值正确。<br>2. 客户端 set 后，服务端或再次 get 能反映新值。<br>3. 多类型参数读写均正确。 | PASS |
| SegarTestCase017 | ROS2 Talker → Segar Listener | 1. 启动 ROS2 demo talker（发布到 rt/chatter，String 类型）。<br>2. 创建 Segar 节点，创建 Reader 订阅 rt/chatter，消息类型为 String。<br>3. 在 Reader 回调中统计接收消息数量与内容（如 data 字段）。<br>4. 等待一段时间或达到预期条数后，校验接收条数与 talker 发送频率×时间相符，内容与 ROS2 发送一致。 | 1. Segar Reader 能接收到 ROS2 talker 发布的消息。<br>2. 接收消息条数符合预期。<br>3. 消息内容（如 String.data）与 ROS2 发送一致。 | PASS |
| SegarTestCase018 | Segar Talker → ROS2 Listener | 1. 创建 Segar 节点，创建 Writer 向 rt/chatter 发布 String 消息，按固定频率或条数发布。<br>2. 启动 ROS2 demo listener（订阅 chatter）。<br>3. 校验 ROS2 端接收到的消息条数与内容（如 data）与 Segar 发送一致。 | 1. ROS2 listener 能接收到 Segar 发布的消息。<br>2. 接收条数与 Segar 发送条数一致。<br>3. 消息内容与 Segar 发送一致。 | PASS |
| SegarTestCase019 | 服务 AddTwoInts 同步调用 | 1. 创建服务端节点，注册 AddTwoInts 服务，处理逻辑为 response.sum = request.a + request.b。<br>2. 创建客户端节点，创建 AddTwoInts Client，WaitForService 后多次调用 SyncSendRequest(a, b)，每次 a、b 按测试数据设置（如 a=seq, b=seq*2）。<br>3. 每次校验 response 非空且 response.sum() == a + b；同时校验单次请求响应时间（如 ≤10ms）。<br>4. 校验服务端处理请求次数与客户端发送次数一致。 | 1. 每次 SyncSendRequest 均返回非空 response。<br>2. response.sum() 等于 request.a + request.b。<br>3. 响应时间在约定阈值内（如 ≤10ms）。<br>4. 服务端处理次数与客户端请求次数一致。 | PASS |
| SegarTestCase020 | 服务 HelloWorld 同步调用 | 1. 创建 HelloWorld 服务端，处理逻辑中返回递增 index 或约定字段。<br>2. 创建客户端，多次 SyncSendRequest，每次校验 response 中 index、message 等字段与预期一致。<br>3. 校验所有请求均成功且 response 内容正确。 | 1. 每次请求均得到非空 response。<br>2. response 各字段（如 index、message）与服务端逻辑一致。<br>3. 多轮请求结果均正确。 | PASS |
| SegarTestCase021 | 服务 AddTwoInts 异步调用 | 1. 创建 AddTwoInts 服务端与客户端；客户端使用 AsyncSendRequest 发送多笔请求，在回调中校验 response.sum() == request.a + request.b，并统计回调次数。<br>2. 主流程等待所有异步回调完成（或超时）。<br>3. 校验回调次数等于发送请求数，且每笔 response 正确。 | 1. 每笔异步请求的回调均被调用。<br>2. 回调中 response.sum() 正确。<br>3. 回调总次数与发送请求数一致。 | PASS |
| SegarTestCase022 | Topic IDL HelloWorld 收发 | 1. 创建 Writer 节点与 Reader 节点，topic 为 chatter_idl，类型为 HelloWorld（IDL）。<br>2. Writer 按序号 seq 循环构造 HelloWorld：设置 ready、各整型/浮点、字符串、宽字符串、raw_bytes、samples 数组、嵌套 detail 等字段。<br>3. Reader 在回调中按 seq 校验接收消息的 ready、整型、浮点、note、wide_note、raw_bytes、samples、detail.detail_id/detail.description 与发送一致。 | 1. Reader 能接收全部发送条数。<br>2. 每条消息的 ready、整型、浮点、字符串、数组、嵌套结构等与 Writer 发送一致。 | PASS |
| SegarTestCase023 | Topic Proto Chatter 收发 | 1. 创建 Writer/Reader，topic 为 chatter_proto，类型为 Chatter（Proto）。<br>2. Writer 设置 timestamp、lidar_timestamp、seq、content 后发布；Reader 在回调中校验 timestamp>0、seq、content 与发送一致，并校验 lidar_timestamp ≥ timestamp。 | 1. 接收条数与发送一致。<br>2. timestamp、seq、content 正确。<br>3. lidar_timestamp ≥ timestamp。 | PASS |
| SegarTestCase024 | Topic IDL String 收发 | 1. 创建 Writer/Reader，topic 为 chatter_string，类型为 std_msgs/msg/String。<br>2. Writer 按序号将 data 设为 to_string(seq) 后发布。<br>3. Reader 校验接收消息的 data 与对应 seq 的字符串一致。 | 1. 接收条数与发送一致。<br>2. 每条消息的 data 与发送序号对应一致。 | PASS |
| SegarTestCase025 | Topic RawMessage（Proto）收发 | 1. 创建 Writer，使用 Proto 类型（如 Chatter）向 chatter_raw_proto 发布消息。<br>2. 创建 Reader，使用 RawMessage 类型订阅该 topic，在回调中通过 ProtobufFactory 按 message_type 反序列化得到 Proto，校验各字段（如 seq、content、timestamp）与发送一致。<br>3. 校验接收条数与发送一致。 | 1. RawMessage 能接收并解析出 Proto。<br>2. 解析后的 Proto 各字段与发送一致。<br>3. 接收条数与发送一致。 | PASS |
